# Astra 🏹 (अस्त्र)

**A plugin for the complete development lifecycle.**

Works with [Claude Code](https://claude.ai/code) and [Cursor](https://cursor.com). Same commands, agents, and skills — one plugin, both editors.

```
Idea → Spec → Design → Plan → Implement → Review → Ship
```

**Or automate the whole thing:**
```
/astra:forge "your feature description"
```

Cross-project. Stack-agnostic. Works in any codebase.

---

## Prerequisites

| Requirement | Why |
|---|---|
| [Claude Code](https://claude.ai/code) or [Cursor](https://cursor.com) | Astra works as a plugin for both |
| `git` | Cloning Astra, version control in `/astra:ship` |
| [`gh` CLI](https://cli.github.com/) *(optional)* | PR creation in `/astra:ship` — ship works without it, just skips the PR step |
| `node` / `npx` | MCP servers (Context7, Playwright, DeepWiki) |

---

## Installation

One command — auto-detects your editor (Claude Code, Cursor, or both):

```bash
curl -fsSL https://raw.githubusercontent.com/Prateek080/astra/main/install.sh | bash
```

That's it. The installer clones Astra, detects your editor, and configures everything.

<details>
<summary><b>Install for a specific editor</b></summary>

```bash
# Claude Code only
curl -fsSL https://raw.githubusercontent.com/Prateek080/astra/main/install.sh | bash -s -- --claude

# Cursor only
curl -fsSL https://raw.githubusercontent.com/Prateek080/astra/main/install.sh | bash -s -- --cursor
```

</details>

<details>
<summary><b>Manual installation</b></summary>

```bash
git clone https://github.com/Prateek080/astra.git ~/astra

# Claude Code — add to your shell config:
alias claude="claude --plugin-dir ~/astra"

# Cursor — run the install script:
bash ~/astra/scripts/install-cursor.sh
```

</details>

<details>
<summary><b>Update & Uninstall</b></summary>

```bash
# Update to latest
cd ~/astra && git pull

# Uninstall
~/astra/install.sh --uninstall
```

</details>

**Verify:** Run `/help` inside your editor — you should see all `/astra:*` commands.

> **For teams:** Each developer installs Astra and picks their editor. The project-specific files generated by `/astra:init` (`CLAUDE.md`, `.claude/rules/`, `.cursor/rules/`) get committed to git — that's how the team shares project context. One developer runs `/astra:init` and commits the result; others get project context via git. Create SPEC.md, DESIGN.md, and PLAN.md on feature branches to avoid conflicts between developers working on different features.

---

## Setup

### `/astra:setup` — Configure your machine *(run once)*

```
/astra:setup
```

Configures your global environment with stack-agnostic coding principles and best practices:

| What it configures | Why |
|---|---|
| `~/.claude/settings.json` | Sets effort to high (deeper thinking) + auto-compact at 80% (prevents context from running out) |
| `~/.claude/CLAUDE.md` | Your personal coding standards — loaded into every session across all projects |
| `/permissions`, `/sandbox`, `/statusline` | Walks you through these interactive commands step by step *(Claude Code only — Cursor manages these in Settings UI)* |

### `/astra:init` — Set up your project *(run once per codebase)*

```
/astra:init
```

Scans your project (README, configs, directory structure), asks 2 clarifying questions, then generates:

| What it generates | Purpose |
|---|---|
| `CLAUDE.md` | Project summary — stack, structure, dev commands, conventions. Loaded every session. |
| `.claude/rules/backend.md` | Backend conventions — *"follow route pattern in `app/api/users.py`, use ORM models, add tests"* |
| `.claude/rules/frontend.md` | Frontend conventions — *"reuse `components/ui/`, follow existing data fetching patterns"* |

Rules reference real files from *your* codebase, not generic advice. Only creates rules for directories that exist.

### What files does Astra create in your project?

| File | Created by | Commit to git? |
|---|---|---|
| `CLAUDE.md` | `/astra:init` | **Yes** — team shares project context |
| `.claude/rules/*.md` | `/astra:init` | **Yes** — Claude Code conventions |
| `.cursor/rules/*.mdc` | `/astra:init` | **Yes** — Cursor conventions |
| `PRODUCT.md` | `/astra:forge` | **Yes** — living product context, read by all agents |
| `SPEC.md` | `/astra:spec` or `/astra:forge` | Optional — useful as documentation |
| `DESIGN.md` | `/astra:design` or `/astra:forge` | Optional — useful for design reference |
| `PLAN.md` | `/astra:plan` or `/astra:forge` | Optional — useful for tracking |
| `docs/specs/*.md` | Auto-archived by `/astra:spec` and `/astra:forge` | Optional — previous feature specs |
| `docs/designs/*.md` | Auto-archived by `/astra:design` and `/astra:forge` | Optional — previous feature designs |
| `docs/plans/*.md` | Auto-archived by `/astra:spec` and `/astra:forge` | Optional — previous feature plans |
| `docs/solutions/*.md` | `/astra:compound` | Optional — project knowledge base |
| `docs/.agent-memory/*.md` | Agents (auto) | **No** — personal learnings, add to `.gitignore` |

**You're ready to build.**

---

## Choosing the Right Workflow

Not every task needs the full workflow. Match the commands to the size of the task.

| Size | Example | Workflow |
|---|---|---|
| **Tiny** | Typo, rename, config change | Just ask Claude directly |
| **Small** | Single-file fix, add a field | Just ask Claude → optionally `review` |
| **Medium** | Feature across 2-5 files | `plan` → `implement` → `review` → `ship` |
| **Large** | New feature, unclear scope | `spec` → `design` → `plan` → `implement` → `review` → `ship` |
| **End-to-End** | Full automation with review gates | **`forge "feature description"`** |
| **Cleanup** | Refactor, optimize, pay debt | `debt-audit` → `plan` → `implement` → `review` → `ship` |

> All commands are prefixed with `/astra:` — e.g., `/astra:plan`

**Tips:**
- **Review per phase, not just at the end.** For medium/large tasks, run `/astra:review` after each implementation phase rather than reviewing all changes at once.
- **Plans aren't set in stone.** If the spec changes or a phase reveals a problem, update SPEC.md or PLAN.md and re-run the corresponding command. See `/astra:workflow-guide` for details.
- **Phase tracking is automatic.** `/astra:implement` marks completed phases in PLAN.md, so you can start a fresh session and pick up where you left off.

---

## Command Reference

### `/astra:forge` — Automated end-to-end pipeline

| | |
|---|---|
| **Input** | Feature description |
| **Output** | Working code + updated `PRODUCT.md` |
| **Next** | `/astra:ship` |

```
/astra:forge "user auth with Google and GitHub OAuth"
/astra:forge "add a notifications system"
```

The fully automated pipeline. Give it a feature description and it orchestrates the entire flow:

1. **PM agent** interviews you → produces `SPEC.md` with numbered requirements → you approve
2. **Designer agent** explores your codebase → produces `DESIGN.md` with component/API specs → you approve
3. **Planner agent** creates phased `PLAN.md` with full traceability → you approve
4. **Implementer** builds phase by phase with **reviewer** checking each phase
5. Final review validates all acceptance criteria → `PRODUCT.md` updated → artifacts archived

Every requirement is traced through every stage: R1 → D-R1 → Phase 1 Task 2 → Test. Nothing falls through the cracks.

`PRODUCT.md` is a living document that tracks your product's current state — features, design system, API surface. On subsequent forge runs, agents read it to build incrementally on what exists.

---

### `/astra:spec` — Define what to build

| | |
|---|---|
| **Input** | Feature description (optional) |
| **Output** | `SPEC.md` |
| **Next** | Fresh context → `/astra:design` or `/astra:plan` |

```
/astra:spec "user auth with Google and GitHub OAuth"
/astra:spec
```

Delegates to a PM subagent with a **pm-framework skill** covering PRD methodology, RICE prioritization, and Given/When/Then acceptance criteria. Interviews you about the feature — problem, users, scope, constraints, edge cases, non-requirements. Challenges your assumptions and digs into the hard parts. Produces a structured spec with numbered requirements (R1, R2...) and testable acceptance criteria.

---

### `/astra:design` — Create an implementable design

| | |
|---|---|
| **Input** | `SPEC.md` or feature description |
| **Output** | `DESIGN.md` |
| **Next** | Fresh context → `/astra:plan` |

```
/astra:design
/astra:design "redesign the navigation"
```

Delegates to a designer subagent with a **design-system skill**. Explores your codebase to understand existing patterns (components, tokens, API structure, data models), then produces `DESIGN.md` with:
- **Frontend features**: Component specs with exact tokens, states, accessibility, responsive behavior
- **Backend features**: API endpoints, data models, system architecture
- **Full-stack**: Both

Every design element traces to a spec requirement (D-R1 → R1). Reuses existing codebase patterns instead of reinventing.

---

### `/astra:plan` — Break the work into phases

| | |
|---|---|
| **Input** | `SPEC.md`, `DESIGN.md` (optional), or inline description |
| **Output** | `PLAN.md` |
| **Next** | `/astra:implement` |

```
/astra:plan
/astra:plan "add pagination to the transactions API"
```

Delegates codebase exploration to a subagent (keeps your context clean). Identifies existing patterns, files to modify vs create, and test infrastructure. Produces a phased plan where each phase has scope, tasks, a test gate, and a parallel flag.

---

### `/astra:implement` — Build phase by phase

| | |
|---|---|
| **Input** | `PLAN.md` or phase number |
| **Output** | Working code with passing tests |
| **Next** | `/astra:review` |

```
/astra:implement
/astra:implement "phase 3"
```

Executes phases from the plan. Before writing new code, searches the codebase for existing patterns to reuse. Runs the test gate after each phase — won't proceed until it passes. Phases marked `Parallel: yes` are automatically grouped and executed concurrently via parallel subagents. Suggests clearing context between phase groups. Stops and asks after 2 failed attempts instead of guessing.

---

### `/astra:review` — Catch issues before shipping

| | |
|---|---|
| **Input** | Uncommitted changes, file path, or git ref |
| **Output** | Findings by severity (Critical / Warning / Suggestion) |
| **Next** | Fix criticals → `/astra:ship` |

```
/astra:review
/astra:review src/auth/
/astra:review main..HEAD
```

Delegates to a reviewer subagent with a **50+ item checklist** covering security, performance, quality, reusability, tech debt, accessibility, and API design. Each finding includes the file, line number, what's wrong, and how to fix it.

---

### `/astra:ship` — Commit and open a PR

| | |
|---|---|
| **Input** | Base branch (optional, defaults to main) |
| **Output** | PR URL |

```
/astra:ship
/astra:ship develop
```

Say "just commit" or "skip the PR" for partial flows — ship adapts to your intent.

Runs pre-flight checks before committing:

| Check | What it catches |
|---|---|
| Test suite | Failing tests |
| Linter/formatter | Style violations |
| Secrets scan | API keys, credentials, `.env` files in diff |
| Debug artifacts | `console.log`, `print()`, `debugger` statements |

Stages files explicitly (never `git add .`), commits with a clean imperative message, pushes, and opens a PR via `gh`.

---

### `/astra:debug` — Find and fix the root cause

| | |
|---|---|
| **Input** | Error message or description |
| **Output** | Root cause analysis + verified fix + regression test |
| **Next** | `/astra:review` → `/astra:ship` |

```
/astra:debug "TypeError: cannot read property 'id' of undefined"
/astra:debug
```

Delegates to a debugger subagent. Forms hypotheses, tests them systematically (no guess-and-check). Implements the smallest fix that addresses the root cause, writes a regression test, and reports: root cause, evidence, what was changed, and how to prevent similar bugs.

---

## Additional Skills

| Skill | What it does | When to use |
|---|---|---|
| **`/astra:compound`** | Documents a solved problem to `docs/solutions/` with searchable categorization. Builds a project knowledge base over time. | After fixing a non-trivial bug or making an architectural decision |
| **`/astra:debt-audit`** | Scans for dead code, orphaned TODOs, deprecated patterns, missing tests, duplication. Outputs a prioritized report. | Before a refactor or cleanup sprint |
| **`/astra:workflow-guide`** | Explains which commands to use for new projects, new features, and optimizations. | When you're unsure which workflow to follow |
| **`/astra:retrospective`** | Compares spec and plan to what was actually built. Suggests improvements for your workflow. | After shipping a feature |

---

## Context Management

> **Why this matters:** AI coding assistants have a finite context window. As the conversation grows, older instructions get compressed or lost. When context fills up, the assistant forgets your conventions, misses details, and quality drops. **Managing context is the single most important habit for getting good results.**

| Situation | Claude Code | Cursor |
|---|---|---|
| Finished a task, starting something new | `/clear` | Start a new chat |
| Between implementation phases | `/clear` | Start a new chat |
| Same mistake keeps repeating | `/clear` + rephrase | New chat + rephrase |
| Long session, can't clear yet | `/compact` (do this around 50% usage) | Context usage visible in UI — start a new chat when heavy |
| Want to monitor usage | `/statusline` (set up once during `/astra:setup`) | Context usage visible in Cursor UI |

**Built-in protections:**
- Astra commands delegate heavy exploration to **subagents** — their file reads happen in a separate context, not yours.
- Auto-compaction set to **80%** during `/astra:setup` (default 95% is too late).

---

## Under the Hood

### Agents

Commands delegate to specialized agents, each running in its own context window.

| Agent | Role | Skill | Tools | Learns |
|---|---|---|---|---|
| **pm** | Product discovery, requirements, PRD | pm-framework | Read-only | Yes |
| **designer** | UI/UX specs, API design, data models | design-system | Read-only | Yes |
| **planner** | Explores codebase, creates plans | plan-template | Read-only | Yes |
| **implementer** | Writes code, runs tests | implementation-patterns | Read + Write | Yes |
| **reviewer** | 50+ item review checklist | review-checklist | Read-only | Yes |
| **debugger** | Root cause analysis and fix | debugging-methodology | Read + Write | Yes |

Learning agents accumulate knowledge across all your projects — the reviewer remembers recurring issues, the debugger remembers common root causes. Memory persists natively in Claude Code. Cursor users can add persistence via an MCP memory server (see [Cursor guide](docs/cursor.md#agent-memory-on-cursor)). The implement command automatically detects parallel phases in `PLAN.md` and spawns subagents to execute them concurrently — both editors support worktree-based parallel execution.

### MCP Servers *(auto-loaded)*

| Server | Purpose |
|---|---|
| **Context7** | Up-to-date library docs. Prevents hallucinated APIs. |
| **Playwright** | Browser automation for testing UI. |
| **DeepWiki** | Documentation for any GitHub repo. |

---

## Project Structure

```
astra/
├── .claude-plugin/plugin.json       Claude Code manifest (keep in sync with Cursor manifest)
├── .cursor-plugin/plugin.json       Cursor manifest (keep in sync with Claude Code manifest)
│
├── commands/                        /astra:* commands
│   ├── setup.md                     One-time global config
│   ├── init.md                      Per-project setup
│   ├── forge.md                     Automated end-to-end pipeline
│   ├── spec.md                      Feature discovery (delegates to PM agent)
│   ├── design.md                    Design document (delegates to designer agent)
│   ├── plan.md                      Phased planning
│   ├── implement.md                 Phase-by-phase execution
│   ├── review.md                    Code review
│   ├── ship.md                      Commit + PR
│   ├── debug.md                     Root cause analysis
│   ├── compound.md                  Knowledge compounding
│   ├── debt-audit.md                Tech debt scanner
│   ├── workflow-guide.md            Usage guide
│   └── retrospective.md            Post-feature assessment
│
├── agents/                          Specialized subagents
│   ├── pm.md                        Product Manager — requirements, PRD
│   ├── designer.md                  Designer — UI specs, API design, data models
│   ├── planner.md                   Codebase exploration, plan creation
│   ├── implementer.md               Code execution, pattern reuse
│   ├── reviewer.md                  Review checklist
│   └── debugger.md                  Root cause analysis, regression prevention
│
├── skills/                          Reusable knowledge (preloaded into agents)
│   ├── pm-framework/                PRD methodology → PM agent
│   ├── design-system/               Design methodology → designer agent
│   ├── implementation-patterns/     Code patterns, test-first → implementer agent
│   ├── debugging-methodology/       Root cause analysis → debugger agent
│   ├── review-checklist/            50+ checks → reviewer agent
│   ├── plan-template/               Plan structure → planner agent
│   ├── debt-audit/                  Tech debt scanner (user-invocable)
│   ├── workflow-guide/              Usage guide (user-invocable)
│   └── retrospective/              Post-feature assessment (user-invocable)
│
├── docs/
│   └── cursor.md                    Cursor-specific setup and differences
│
├── scripts/
│   └── install-cursor.sh            Cursor install script
│
├── install.sh                       One-command installer (curl | bash)
├── .mcp.json                        Context7, Playwright, DeepWiki
└── LICENSE                          MIT
```
