---
description: Update Astra to the latest version
---

# Update Astra

Pull the latest changes and re-register the plugin.

## Steps

1. Run `git -C ~/astra pull`
2. Run `bash ~/astra/install.sh`
3. Print the output to the user.
