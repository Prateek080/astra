{
  "hooks": {
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v osascript >/dev/null 2>&1; then osascript -e 'display notification \"Astra needs your attention\" with title \"Astra\"'; elif command -v notify-send >/dev/null 2>&1; then notify-send 'Astra' 'Astra needs your attention'; fi"
          }
        ]
      }
    ]
  }
}
